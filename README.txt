FINAL DEPLOYMENT PACKAGE — Mohammad Yazdani

Files are ready to deploy as a static site.

Required before public launch:
- Put the real `CV - Mohammad Yazdani.pdf` beside index.html.
- Replace the root-relative canonical with the exact live HTTPS URL only if the site is not served from the domain root.
- For absolute Open Graph URLs and sitemap.xml, use the actual live domain. Do not publish the example sitemap unchanged.
- Run Lighthouse and Google Search Console URL Inspection on the live HTTPS URL.
GitHub Pages entry point: index.html
